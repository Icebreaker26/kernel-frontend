VITE_API_BASE_URL=http://localhost:4000/api

# Dirección del sitio público nuevo (remake de WordPress). Solo se define cuando el sitio viva en otro dominio;
# vacía = mismo dominio. Las rutas (/pqrs, /pagos...) no cambian.
# VITE_URL_SITIO=https://cooperativaprogresemos.coop
